import {useEffect,useState} from 'react';export function usePathname(){const[p,sp]=useState(window.location.pathname);useEffect(()=>{const f=()=>sp(window.location.pathname);addEventListener('popstate',f);return()=>removeEventListener('popstate',f)},[]);return[p,sp]} export function Link({to,children,className}){const c=e=>{e.preventDefault();history.pushState({},'',to);dispatchEvent(new PopStateEvent('popstate'))};return <a href={to} onClick={c} className={className}>{children}</a>;}